safety:
  name: supervised_hardware_motion_limits
  max_velocity_scale: 0.10
  max_acceleration_scale: 0.10
  planning_timeout_s: 5.0
  require_collision_checking: true
  allow_hardware_execution: true
  notes: Supervised arm-only hardware demo limits. Use only with pendant/e-stop operator present.
